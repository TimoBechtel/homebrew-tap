name: Update Homebrew tap

on:
  schedule:
    - cron: "23 4 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-homebrew-tap
  cancel-in-progress: false

jobs:
  update:
    runs-on: macos-latest

    steps:
      - name: Check out tap
        uses: actions/checkout@v4

      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@main

      - name: Cache Homebrew Bundler RubyGems
        id: cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.set-up-homebrew.outputs.gems-path }}
          key: ${{ runner.os }}-rubygems-${{ steps.set-up-homebrew.outputs.gems-hash }}
          restore-keys: ${{ runner.os }}-rubygems-

      - name: Install Homebrew Bundler RubyGems
        if: steps.cache.outputs.cache-hit != 'true'
        run: brew install-bundler-gems

      - name: Update cmux Chromium cask
        run: scripts/update-cmux-chromium-cask
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Update hey-comma formula
        run: scripts/update-hey-comma-formula
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Validate cask and formula
        run: |
          brew style --cask timobechtel/tap/cmux-chromium
          brew audit --cask --no-signing --arch=arm timobechtel/tap/cmux-chromium
          brew livecheck --cask timobechtel/tap/cmux-chromium
          brew style --formula timobechtel/tap/hey-comma
          brew audit --formula --strict --online timobechtel/tap/hey-comma
          brew livecheck --formula timobechtel/tap/hey-comma

      - name: Check for update
        id: update
        run: |
          if git diff --quiet -- Casks/cmux-chromium.rb Formula/hey-comma.rb; then
            echo "No tap update."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Configure git
        if: steps.update.outputs.changed == 'true'
        uses: Homebrew/actions/git-user-config@main

      - name: Commit update
        if: steps.update.outputs.changed == 'true'
        run: |
          git add Casks/cmux-chromium.rb Formula/hey-comma.rb
          git commit -m "Update Homebrew tap packages"

      - name: Push update
        if: steps.update.outputs.changed == 'true'
        uses: Homebrew/actions/git-try-push@main
        with:
          token: ${{ github.token }}
          directory: ${{ github.workspace }}
          branch: main
